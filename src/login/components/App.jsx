import * as React from "react";

const App = () => {

  // The list items are static and won't change at runtime,
  // so this should be an ordinary const, not a part of state.
  return (
    <div>
      <p>Test app component</p>
    </div>
  );
};

export default App;
